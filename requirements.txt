fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
python-multipart==0.0.12

# LangChain RAG stack (let pip resolve compatible versions)
langchain>=0.3.9
langchain-groq>=0.2.3
langchain-community>=0.3.9
langchain-core>=0.3.29
langchain-text-splitters>=0.3.2

# Vector store (pre-built Windows wheels, no C++ compiler needed)
faiss-cpu==1.9.0.post1

# Embeddings (free local model — no API key needed)
sentence-transformers==3.3.1

# HTTP client for weather
httpx==0.28.1

# Web search tool (free, no API key needed)
duckduckgo-search>=6.3.0

# Document parsing (PDF, Word, images)
pypdf>=4.0.0
python-docx>=1.1.0
Pillow>=10.0.0
pytesseract>=0.3.10
